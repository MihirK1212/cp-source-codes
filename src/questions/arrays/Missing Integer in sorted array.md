# Missing Integer in Sorted Array

## Problem Description

This problem is from GeeksforGeeks: [Find the Missing Number in a Sorted Array](https://www.geeksforgeeks.org/find-the-missing-number-in-a-sorted-array/)

Given a sorted array of `n-1` distinct integers in the range of 1 to `n`, the task is to find the missing integer in the array.

## C++ Solution

```cpp
// No code solution provided in the original file.
// A typical solution involves iterating through the array or using binary search,
// checking if `arr[i] - i - 1` is non-zero, or summing expected vs actual.
```