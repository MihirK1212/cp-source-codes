# Sorting static arrays

Static Arrays
To sort static arrays, use sort(arr, arr + N) where N is the number of elements to be sorted.
The range can also be specified by replacing arr and arr + N with the intended range. For example, sort(arr + 1, arr + 4) sorts indices [1, 4)
